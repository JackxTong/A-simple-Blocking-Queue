# Java BlockingQueue Demo

Simple producer–consumer example using a custom blocking queue.

## How to run
```bash
javac src/*.java
java Main
```


FIELDLIKE[leg_type; "*EUR*"] | FIELDLIKE[leg_type; "*GBP*"]
